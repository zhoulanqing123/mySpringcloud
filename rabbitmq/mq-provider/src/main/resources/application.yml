spring:
  main:
    allow-circular-references: true
  application:
    name: mq-provider
  rabbitmq:
    port: 5672
    host: 127.0.0.1
    username: guest
    password: guest
    virtual-host: /
#    publisher-confirm-type: correlated
#    publisher-returns: true
server:
  port: 8080
