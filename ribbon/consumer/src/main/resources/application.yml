#?????汾
server:
  port: 8083 #??????????????
spring:
  application:
    name: consumer
eureka:
  client: #?????????????
    service-url: #????????
      deaflutZone: http://localhost:8761/eureka
    register-with-eureka: true #???????eureka-server???
    fetch-registry: true  #???????????????б?
    registry-fetch-interval-seconds: 15 #??????????б?????????  ???X???????????????б? ???????????? ?????????
  instance:
    hostname: localhost
    instance-id: ${eureka.instance.hostname}:${spring.application.name}:${server.port}
    lease-renewal-interval-in-seconds: 10
    prefer-ip-address: true